version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  pre_build:
    commands:
      - chmod +x gradlew
      - chmod +x scripts/*.sh

  build:
    commands:
      - cd back
      - ./gradlew clean build -x test

artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - build/libs/*.war
